# MuktoAin — Teammate Dependency & Execution Flow

> **Progress Tracking:** To update progress, edit this file and change `[ ]` to `[x]` on completed tasks, and wrap the completed line in `~~strikethrough~~` (e.g. `- [x] ~~**[T-1.1]** ...~~`).
> **Initials & Step Prefix Key:**
> - **`[S-Setup.#]`** & **`[S-#.#]`** = **Shads** (Project Lead · `Shads_plan.md`)
> - **`[T-#.#]`** = **Tultul** (Data Foundation & Search · `Tultul_plan.md`)
> - **`[A-#.#]`** = **Arpita** (Document Generation, Review Gate & Admin · `Arpita_plan.md`)
> - **`[E-#.#]`** = **Erin** (Frontend Razor Views & Mock Integration · `Erin_plan.md`)

---

## 🏁 Checkpoint 1: Foundation, Schema & Data Ingestion (30%)

### 1. Day 0 Setup & Independent Start (No Cross-Teammate Blockers)
- [x] ~~**[S-Setup.1]** Create GitHub Repository (`muktoAin-ISD`) — *Shads* `[Unblocks: T-1.1, S-Setup.2]`~~
- [x] ~~**[S-Setup.2]** Set Up Branch Protection on `main` — *Shads* `[Unblocks: S-Setup.3]`~~
- [x] ~~**[S-Setup.3]** Create Feature Branches for Each Teammate — *Shads* `[Unblocks: All Teammates]`~~
- [x] ~~**[S-Setup.4]** Assign Teammate Starting Points — *Shads*~~
- [x] ~~**[S-Setup.5]** Prepare Seed Data Files (`districts.json`, `categories.json`, etc.) — *Shads* `[Unblocks: T-1.7]`~~
- [x] ~~**[S-Setup.6]** Set Up Multi-Key Gemini API Key Rotation Config — *Shads* `[Unblocks: S-1.3]`~~
- [x] ~~**[S-Setup.7]** Set Up Qdrant Cloud Cluster — *Shads* `[Unblocks: T-1.11]`~~
- [x] ~~**[S-Setup.8]** Share `appsettings.Development.json.template` — *Shads*~~
- [x] ~~**[S-1.3]** Implement `GeminiClient.cs` with Key Rotation — *Shads* `[Unblocks: S-1.4]`~~
- [x] ~~**[S-1.5]** Define Legal Prompt Templates & Disclaimers (`Disclaimers.cs`) — *Shads* `[Unblocks: S-1.6, S-2.1]`~~
- [x] ~~**[S-1.6]** Implement `DisclaimerInjector.cs` — *Shads* `[Blocked by: S-1.5] [Unblocks: S-2.2]`~~ — implemented with bilingual support (en/bn) and registered in DI; verified by 5 unit tests
- [x] ~~**[S-1.7]** Implement `EncryptionService.cs` (ASP.NET Data Protection API) — *Shads* `[Unblocks: S-2.5]`~~ — implemented with ASP.NET Data Protection API, registered in DI; verified by 4 unit tests (Bangla/English roundtrips, empty/null safety)
- [x] ~~**[E-1.1]** Master Layout `_Layout.cshtml` (Bootstrap 5, Nav, Footer) — *Erin* `[Unblocks: E-1.4]`~~
- [x] ~~**[E-1.2]** `_DisclaimerBanner.cshtml` & `_LanguageToggle.cshtml` — *Erin* `[Unblocks: E-1.1]`~~
- [x] ~~**[E-1.3]** Static Assets (CSS, JS & Noto Sans Bengali Fonts in `wwwroot/`) — *Erin* `[Unblocks: A-2.5]`~~
- [x] ~~**[E-1.4]** Home Controller & Views (Landing page with mock data) — *Erin* `[Blocked by: E-1.1]`~~
- [x] ~~**[E-1.5]** Identity Views (`Login.cshtml`, `Register.cshtml`) — *Erin* `[Blocked by: E-1.1]`~~
- [x] ~~**[E-1.6]** Checkpoint 1 Frontend Exit Gate — *Erin* `[Blocked by: E-1.1 to E-1.5]`~~

### 2. Core Architecture Critical Path (Tultul's First Wave)
- [x] ~~**[T-1.1]** Initialize .NET 8 Solution (`MuktoAin.sln` 4 Projects + References) — *Tultul* `[Blocked by: S-Setup.1] [Unblocks: T-1.2]`~~
- [x] ~~**[T-1.2]** Implement All 9 Enums in `MuktoAin.Domain/Enums/` — *Tultul* `[Blocked by: T-1.1] [Unblocks: T-1.3, A-1.1]`~~
- [x] ~~**[T-1.3]** Implement All 14 Domain Entities in `MuktoAin.Domain/Entities/` — *Tultul* `[Blocked by: T-1.2] [Unblocks: T-1.4, A-1.1]`~~
- [x] ~~**[T-1.4]** Define Repository & Service Interfaces in Domain/Application — *Tultul* `[Blocked by: T-1.3] [Unblocks: T-1.5, A-2.1]` — amended during T-1.12: `IRepository<T>.GetByIdAsync` changed from `int` to `object` (didn't fit AiLog's long PK)~~
- [x] ~~**[T-1.5]** EF Core `AppDbContext.cs` in `MuktoAin.Infrastructure` — *Tultul* `[Blocked by: T-1.3, T-1.4] [Unblocks: T-1.6, S-1.1]`~~
- [x] ~~**[T-1.6]** Manual MSSQL Schema Scripts in SSMS (`scripts/01-14_*.sql`) — *Tultul* `[Blocked by: T-1.3, T-1.5] [Unblocks: S-1.1, T-1.7]`~~
- [x] ~~**[A-1.1]** All DTOs in `MuktoAin.Application/DTOs/` — *Arpita* `[Blocked by: T-1.2, T-1.3] [Unblocks: A-2.1]`~~
- [x] ~~**[A-1.2]** Checkpoint 1 DTOs Exit Gate — *Arpita* `[Blocked by: A-1.1]`~~

### 3. Identity, Data Ingestion & Vector Indexing
- [x] ~~**[S-1.1]** ASP.NET Core Identity Configuration in `MuktoAin.Infrastructure` — *Shads* `[Blocked by: T-1.5, T-1.6] [Unblocks: S-1.2, S-3.6]` — verified against real SQL Server; `AppDbContext` now inherits `IdentityDbContext<User, IdentityRole<int>, int>` (no role/claim tables exist in the SSMS schema by design — authorization runs off `User.Role` enum via `UserRoleClaimsTransformation` in Web/Auth); cookie auth via `AddIdentityCore` + explicit `AddAuthentication(IdentityConstants.ApplicationScheme)`; `UserConfiguration.cs` maps `User.Id` → physical `UserId` column~~ **— [Aug 2026] Registration & Login E2E hardening verified live (SQLEXPRESS, http://localhost:5250):** (1) schema `[dbo].[USER]`/`02_schema.sql` fully match `IdentityUser<int>` — **no schema script change needed**; (2) password policy aligned across `RegisterViewModel` regex, Identity config, and `Login.cshtml` demo-fill hints; (3) `SeedDemoUsers.cs` (Development-only) seeds `citizen@muktoain.bd`/`Citizen@123` + `lawyer@muktoain.bd`/`Lawyer@123` with Pending LawyerProfile `DEMO-BAR-2026-0001`; (4) `IdentityErrorMapper.cs` + `AccountController` give bilingual field-level identity errors and suspended/locked messages; (5) **128 unit tests pass** (10 new AccountController + 8 new IdentityErrorMapper); live E2E confirmed: role logins → 302 to `/Admin/Dashboard` `/Lawyer/Queue` `/Case/Track`, register citizen, register lawyer ± bar no (missing bar = field error), wrong-password, lockout-after-5 (correct pw still locked, `LockoutEnd` set), suspended message, logout clears `MuktoAin.Auth` cookie. **⚠️ OPEN issues found:** (a) **authorization gap** — `// [Authorize(Roles="Admin")]` still commented in `AdminController.cs` (line 5), and `LawyerController`/`CaseController` have no `[Authorize]` gates → any visitor (or unauthenticated) can open `/Admin/Dashboard` & `/Lawyer/Queue` (role claim projection `UserRoleClaimsTransformation` IS ready; gate just needs enabling); (b) UI nav "Logout" (`main.js:444`) is a stub link to `/Account/Login`, not wired to the `[HttpPost] Account/Logout` action. **— [Aug 2026] BOTH OPEN ISSUES RESOLVED + hardened:** (1) role gates enabled — `AdminController` `[Authorize(Roles="Admin")]`, `LawyerController` `[Authorize(Roles="Admin,Lawyer")]`, `CaseController` stays guest-open; `Program.cs` AccessDeniedPath → `/Home/AccessDenied` (403 page, `HomeController`); (2) nav Logout replaced with a real `<form method="post" asp-action="Logout">` (Razor antiforgery token) + `main.js`/`main.css` selector updates. **Live E2E matrix re-verified (single instance, SQL healthy):** admin login → 302 `/Admin/Dashboard`; citizen → 302 `/Case/Track`; lawyer → 302 `/Lawyer/Queue`; unauthenticated `/Admin/Dashboard` & `/Lawyer/Queue` → 302 `/Account/Login?ReturnUrl=...`; citizen on both → 302 `/Home/AccessDenied` (followed = 403 page); lawyer `/Lawyer/Queue` 200 but `/Admin/Dashboard` denied; admin both 200; logout POST (authenticated-context token) → 302 `/` + `MuktoAin.Auth` cleared + subsequent `/Admin/Dashboard` → login redirect; wrong-password → 200 `email or password is incorrect`; register regression ✓; **128 unit tests pass. Also fixed a latent bug:** `UseStatusCodePagesWithReExecute("/Home/Error")` re-executes with the ORIGINAL method but `HomeController.Error` was `[HttpGet]` — any non-GET 4xx/5xx surfaced as a misleading **405**; made `/Home/Error` method-agnostic (route-only) so status-code pages render for POST-triggered errors too. **Root-cause note:** the post-restart universal "405/400 on every POST" was NOT an app regression — the new `_Layout` logout `<form>` emits a second `__RequestVerificationToken`, and the E2E curl harness grabbed two newline-joined tokens (155+1+155=311) which antiforgery correctly rejects; fixed harness extraction (single per-line token + identity-matched context), app unchanged.
- [x] ~~**[S-1.2]** `SeedAdminUser.cs` Startup Seeding — *Shads* `[Blocked by: S-1.1]` — idempotent; credentials from `SeedAdmin` config section (template updated); verified live: seeded `admin@muktoain.bd` (UserId=1, Role=Admin) on real SQL Server — ⚠️ **open env issue found during this verification:** dev machine cannot bind ports 5080/5082 (Windows excluded port range, not a code bug); diagnosis + fix options in Shads_plan Step 1.2 `[ENV-BUG][OPEN]`; also note the local admin row was seeded with the bootstrap default password BEFORE `SeedAdmin:Password` was configured — reset/delete that row or sign in with the default~~ — **[Aug 2026] Stale bootstrap-password admin row (UserId=1) deleted live; on next startup `SeedAdminUser` re-seeded `admin@muktoain.bd` with `SeedAdmin:Password` (`muktoAin@123`), now UserId=2.**
- [x] ~~**[T-1.7]** Seed Data Loaders (`SeedDistricts`, `SeedCategories`, `SeedScenarioMappings`) — *Tultul* `[Blocked by: S-Setup.5, T-1.6] [Unblocks: T-1.8]` — note: no `SeedDocumentTypes` exists because `DocumentType` is an enum column, not a lookup table (scripts/02_schema.sql has no `DOCUMENT_TYPE` table); `SeedScenarioMappings` safely no-ops until `ACT_SECTION` rows exist (T-1.8)~~
- [x] ~~**[T-1.8]** 1,484 Bangladesh Acts Ingestion Pipeline (`ActImportService.cs`) — *Tultul* `[Blocked by: T-1.6, T-1.7] [Unblocks: T-1.9]` — verified against real SQL Server: 1,484 acts, 35,633 sections, 14,523 footnotes imported; idempotent restart confirmed (0 duplicates); unblocked all 26 `SeedScenarioMappings` rows in the same run~~
- [x] ~~**[T-1.9]** Legal Section Chunking Pipeline (`LegalChunkingService.cs`) — *Tultul* `[Blocked by: T-1.8] [Unblocks: S-1.8]` — verified against real SQL Server: 35,633 sections chunked into 42,858 rows; idempotent restart confirmed. Caught and fixed a runaway-split bug during verification (452 sections producing thousands of 1-char-shrinking duplicate chunks each) by requiring a found boundary to land at least halfway into the target window before using it~~
- [x] ~~**[T-1.10]** Manual SQL Server Full-Text Search (FTS) SSMS Script — *Tultul* `[Blocked by: T-1.6] [Unblocks: T-2.2]`~~
- [x] ~~**[T-1.11]** `QdrantVectorStore.cs` (.NET SDK Vector Implementation) — *Tultul* `[Blocked by: S-Setup.7, T-1.4] [Unblocks: S-1.8]` — verified against the live Qdrant Cloud cluster: EnsureCollectionAsync/UpsertAsync/SearchAsync/DeleteAsync round-tripped correctly (collection `act_section_chunks_dev`, size 768, Cosine distance). Used `QueryAsync` instead of `SearchAsync`~~
- [x] ~~**[T-1.12]** MSSQL Repository Implementations (Parameterized SQL) — *Tultul* `[Blocked by: T-1.4, T-1.6] [Unblocks: T-1.13, A-2.1]` — verified against real SQL Server; also added ScenarioMappingRepository for completeness and fixed `IRepository<T>.GetByIdAsync` (see T-1.4 note)~~
- [x] ~~**[T-1.13]** DI Registration in `Program.cs` — *Tultul* `[Blocked by: T-1.11, T-1.12] [Unblocks: S-1.9]` — registered generic `IRepository<>`/`Repository<>` plus the 5 dedicated repository interfaces; verified with a temporary `ValidateOnBuild = true` startup run (no consumers exist yet to exercise these via normal resolution) confirming the whole DI graph resolves cleanly, then reverted the temp flag~~
- [x] ~~**[T-1.14]** Data Layer Unit Tests (`MuktoAin.UnitTests`) — *Tultul* `[Blocked by: T-1.12]` — 14 tests, all passing on EF InMemory; FromSqlRaw/CONTAINSTABLE/ExecuteUpdateAsync methods excluded (unsupported by InMemory) and deferred to T-3.3~~
- [x] ~~**[S-1.8]** `EmbeddingBatchJob.cs` (Embed & Index All Chunks into Qdrant) — *Shads* `[Blocked by: T-1.9, T-1.11, S-1.4] [Unblocks: S-1.9, T-2.1]`~~ — implemented as BackgroundService in Infrastructure/VectorStore; upgraded to true batch embedding using Gemini's native `:batchEmbedContents` API and Qdrant `UpsertBatchAsync` (100 chunks per batch request, reducing API calls by ~100x and eliminating 429 rate limit quota exhaustion); SHA-256 incremental hashing; includes live progress tracker `EmbeddingProgressState`, `/Admin/EmbeddingProgress` endpoint, and real-time dashboard UI progress bar; registered in DI; verified by 157 unit tests + integration tests passing cleanly.
- [x] ~~**[T-1.15]** Checkpoint 1 Data Foundation Exit Gate — *Tultul* `[Blocked by: T-1.1 to T-1.14]` — all of T-1.1–T-1.14 confirmed done; full solution build clean (6 projects incl. tests), 14/14 unit tests pass, end-to-end app startup verified against real SQL Server + live Qdrant with no errors, and a direct SQL check confirms data integrity: 1,484 Acts, 35,633 Sections, 42,858 Chunks, 64 Districts, 4 Categories, FTS catalog fully indexed (35,633 items = full section count)~~
- [x] ~~**[S-1.9]** Checkpoint 1 Overall RAG Ingestion Smoke Test Exit Gate — *Shads* `[Blocked by: S-1.8, T-1.13, T-2.1]`~~ — end-to-end RAG retrieval smoke tests implemented in `tests/MuktoAin.IntegrationTests/AiPipeline/RagRetrievalSmokeTests.cs` and Gemini client multi-key rotation unit tests in `tests/MuktoAin.UnitTests/Services/GeminiClientTests.cs`; verified vector-primary retrieval with Labour Act query, FTS fallback on empty vector results, and Gemini key rotation on 429 quota exhaustion; all 93 tests passing across unit and integration test suites.
- [x] ~~**[FIX-DB-1]** MuktoAin DB Timeout / Error-Flood Root Cause Fix (AUTO_CLOSE + RAM exhaustion) — *Shads* `[Fixed: 2026-09-02]`~~ — root cause: `MuktoAin` DB had `AUTO_CLOSE ON` (SQL Express default) on a machine with chronic 8GB RAM exhaustion, causing per-connection cold DB restarts, 20s+ recoveries, `RECOVERY_PENDING` state, login failures, and the EmbeddingBatchJob fatal error flood on every `dotnet watch`. Fixes applied: (1) `ALTER DATABASE MuktoAin SET AUTO_CLOSE OFF` + forced ONLINE on live instance (applied to all user DBs); (2) hardened `scripts/01_init_database.sql` with RCSI + AUTO_CLOSE OFF so fresh recreations can't regress; (3) `EnableRetryOnFailure(5x, 10s)` + `CommandTimeout(60)` on EF DbContext in Program.cs; (4) EmbeddingBatchJob upgraded to resilient batch loop (batch embedding via `:batchEmbedContents`, per-batch error recovery, 429 pause-and-retry instead of fatal crash); (5) HTTPS redirect dev-gated so the "Failed to determine https port" warning is gone in Development. Verified: DB ONLINE with instant queries (42,858 chunks / 33,510 unembedded), build clean, 157/157 unit tests pass, app serves Home/Login/Admin endpoints at HTTP 200 with embedding job running gracefully.
- [x] ~~**[FIX-EMB-1]** Embedding Ingestion Free-Tier Throughput Optimization — *Shads* `[Fixed: 2026-09-02] [Blocked by: S-1.8]`~~ — root causes of slow embedding + 429 storms: (a) fixed 100-chunk batches under-used the request token ceiling; (b) blind fixed 4.1s delay + 60s 429 sleeps ignored Google's `RetryInfo.retryDelay` and quota scope (per-minute vs per-day); (c) per-project quota means key rotation can't dodge rate limits — all 7 keys share one budget; (d) junk rows (empty/<20-char chunks) and ~500 duplicate-text groups burned quota for zero retrieval value; (e) query embedding used no `taskType` while docs used `RETRIEVAL_DOCUMENT` (asymmetric = recall loss); (f) `Include(Section).ThenInclude(Act)` + ChunkText/ActTitle in Qdrant payload was dead weight (SimilaritySearchService only reads SectionId and re-hydrates from SQL). Fixes: (1) `GeminiClient` parses 429 bodies — new `GeminiQuotaExhaustedException` carries `RetryInfo.retryDelay` + PerDay/PerMinute quotaId scope; (2) query path now sends `taskType=RETRIEVAL_QUERY`; (3) new `EmbeddingQuotaState` AIMD pacing engine (rolling 60s window, halves budget on 429, freezes for exactly Google's retryDelay, +1/min recovery on clean windows, daily `ResetForNewDay`); (4) `EmbeddingBatchJob` reworked: token-packed batches (~160 chunks/req at 25k tokens vs old 100), in-run SHA-256 dedupe (duplicate texts share one embedding + Qdrant point, all rows stamped), auto-resume sleep until midnight-Pacific (DST-aware) on PerDay exhaustion instead of parking forever, real ETA + pacing budget surfaced in `EmbeddingProgressState`/`/Admin/EmbeddingProgress`; (5) `GetUnembeddedChunksAsync` dropped the Include chain; payloads slimmed to ChunkId/SectionId/ChunkOrder; (6) new `GetUnembeddedCountAsync` for ETA; (7) `scripts/07_embedding_optimization.sql` purged 53 junk rows (idempotent, SET options fixed for the filtered index); (8) knobs via `Embedding:BatchMaxTokens`/`BatchMaxTexts`/`CharsPerToken` config. Verified live: 5 batches embedded 800 chunks (160/req) before today's already-burnt daily quota — job correctly detected PerDay, logged the Pacific-midnight auto-resume plan; build clean, 157 unit + 2 integration tests passing (new tests: dedupe collapse, stale-row skip, batch caps, quota AIMD, daily reset, RetryInfo parsing).
- [x] ~~**[FIX-EMB-2]** Embedding Throughput Round 2 — Global Dedupe, Parallel Workers, Full-Request Packing — *Shads* `[Fixed: 2026-09-03] [Blocked by: FIX-EMB-1]`~~ — second-pass optimization of the free-tier embedding pipeline, closing the gaps FIX-EMB-1 left: (a) `BatchMaxTexts` default was still 100 so every request used only ~15k of the 25k-token ceiling (~40% waste per request); (b) dedupe was per-fetch-window — duplicates landing in different windows were re-embedded forever since their rows stayed VectorId/ContentHash NULL; (c) the loop was latency-bound: one embed→Qdrant→SQL round trip in flight at a time capped throughput at ~15-30 req/min regardless of quota headroom; (d) CharsPerToken=4 was optimistic for Bangla (under-estimating tokens causes TPM overshoot → guaranteed 429 storms); (e) the Math.Max(2000,…) token floor could push the last request of a window over TPM; (f) failed batches were re-enqueued unconditionally with no cap → a poisoned batch meant a job that never terminates. Fixes: (1) `EmbeddingBatchJob` restructured into 3 phases — global dedupe scan (keyset-paged via new `GetUnembeddedChunksAfterAsync`, constant memory, one SHA-256 per row, leader/follower split, followers NEVER hit the API), parallel token-packed worker loop (`Embedding:MaxConcurrentBatches`, default 2, all workers share the `EmbeddingQuotaState` gate so total rate never exceeds the pacing budget), then follower stamping (leader's VectorId, zero API calls); (2) `BatchMaxTexts` 100→250 (token ceiling now binds first: ~250 texts ≈ 25k tokens/request); (3) `CharsPerToken` 4→3 (conservative: over-estimating shrinks batches slightly, under-estimating guarantees 429s); (4) `EmbeddingQuotaState` now config-wired (`Embedding:MaxRequestsPerMinute`/`MaxTokensPerMinute`) instead of hardcoded guesses — set the real limits read from 429 quotaId logs; (5) bounded retry: after 5 consecutive transient failures the batch is dropped (rows counted as skipped, stay NULL, retried next startup) so the worker can drain and the job terminates; (6) failed batches re-enqueue for retry (quota 429s, network blips) but only up to the cap; (7) `GeminiClient` supports MRL `outputDimensionality` (768/1536/3072 for gemini-embedding-001) on both embed + batchEmbedContents — 4x smaller responses/upserts at ~1-2% quality cost, config `Gemini:EmbeddingOutputDimensionality`; (8) Program.cs fail-fast guard: `EmbeddingOutputDimensionality` must equal `Qdrant:VectorSize` or startup throws instead of silently corrupting search; (9) removed dead `Embedding:DelayBetweenBatchesMs` config. Verified: build clean, 174/174 unit tests pass (incl. new global-dedupe-across-pages test, duplicate-sharing test reworked for the 3-phase flow). Note: 7 keys in ONE Google Cloud project share ONE quota pool — splitting keys across SEPARATE projects is the only way to multiply free-tier limits.~~ — full audit of 28 views + main.js i18n engine + main.css theme system, then fixes with an explicit Bangla-only exclusion list (legal document bodies, "গণপ্রজাতন্ত্রী বাংলাদেশ" letterhead, doc-meta, DRAFT watermarks, Surface-3 disclaimer stamps, AI-generated RightsExplanation, statute text, decision `<option>` VALUES, bar reg numbers — all intentionally stay Bangla/untranslated). Translation fixes: (1) new `StatusText` helper maps status strings/enums (incl. EditedApproved which exists only as a string) to Bn/En badge text — Track/Queue/Result badges no longer render raw "UnderReview" enums; (2) `Case/Track`, `Lawyer/Queue`, `Lawyer/Review`, `Case/Result`, `Document/Preview` — data-bn/data-en pairs on all UI chrome (headers, KPIs, timeline, buttons, alerts, gate notices); (3) `Shared/Error.cshtml` rewritten into themed bilingual error page (was EN-only scaffold outside the chrome); (4) `Home/Privacy.cshtml` rewritten from template placeholder into a real themed bilingual privacy policy (what's collected / encryption+redaction / user control); (5) NotFound/AccessDenied/ServerError muted lines + buttons paired; (6) main.js: new `data-bn-title`/`data-en-title` tooltip translation handler; (7) `EmbeddingProgressState` gained `LastStatusEn`/`EstimatedCompletionEn` + `SetStatus(bn,en)` — every job status now set bilingually, surfaced through `/Admin/EmbeddingProgress`, and the Admin Dashboard progress widget follows `mkt-lang`. Theme fixes: (8) `color-scheme: light/dark` on both token blocks — native scrollbars/selects/inputs now render correctly in dark mode; (9) h1-h3 color switched from hardcoded `#4d3c10` to `var(--ink-heading)`; (10) citation-chip hover brightens instead of darkening (invisible on dark bg); (11) DRAFT paper-watermark given a dark-theme color so it's visible on the night-library sheet. Verified: build clean, 157 unit + 2 integration tests pass, authenticated smoke of Lawyer/Queue confirmed bilingual headers/KPIs/badges server-rendered (env RAM pressure — the known FIX-DB-1 condition — blocked the final post-fix runtime pass; the changed code path is identical to the verified one).

- [x] ~~**[FIX-EMB-3]** GeminiClient Round-Robin Key Striping for Independent Quota Pools — *Shads* `[Fixed: 2026-09-03] [Blocked by: FIX-EMB-2]`~~ — the 7 API keys come from 7 different Google accounts, so each key's AI Studio default project is a SEPARATE Cloud project = 7 INDEPENDENT free-tier quota pools. The old rotate-on-429 strategy burned pool 1 to exhaustion before touching pool 2, serializing capacity instead of adding it. Fixes: (1) `GeminiClient` now STRIPES requests round-robin across the ring — the key index advances on every successful request (with `AdvancePast` ring hygiene so the last-served key is never re-picked after mid-rotation), spending all pools at the same rate; (2) a 429 parks only the throttled key for exactly Google's `RetryInfo.retryDelay` (escalating on back-to-back 429s; 401/403 parks a dead key 10min) while the next key serves immediately — one throttled pool no longer stalls the fleet; (3) `GeminiQuotaExhaustedException.RetryAfter` now reflects the SOONEST `parkedUntil` across keys (not just the last 429's retryDelay, which describes only one pool) so the job's backoff wakes exactly when the first pool frees; (4) job-level `EmbeddingQuotaState` AIMD still bounds the TOTAL rate, so striping never exceeds the aggregate budget — appsettings raised to `MaxRequestsPerMinute: 700` / `MaxTokensPerMinute: 700000` (7 pools x ~100 RPM); (5) appsettings comment documents the invariant: keys must live in separate Cloud projects, or trim to one key. Verified: build clean, 177/177 unit tests pass, incl. new tests: consecutive-request striping order (key-1→2→3→1…), single-key park with others continuing, all-keys-429 RetryAfter ≤ soonest park, and the pre-existing rotation/fail-fast tests re-verified against the new strategy.~~

- [x] ~~**[FIX-EMB-4]** batchEmbedContents 100-Text Hard Cap Misclassification (400 retried as transient → run aborted, 233 rows stranded) — *Shads* `[Fixed: 2026-09-03] [Blocked by: FIX-EMB-3]`~~ — symptom: every batch returned 400 INVALID_ARGUMENT, the job burned 5 retries × exponential backoff (~3 min) per batch, dropped 233 rows across 2 batches, processed 0. Root cause (verified against the LIVE API with a repro request): `batchEmbedContents` hard-caps at **100 requests per batch** — "at most 100 requests can be in one batch" — so the BatchMaxTexts=250 default from FIX-EMB-2 made EVERY request fail 400. Compounding bug: `EmbeddingBatchJob`'s catch-all treated the permanent 400 as transient (5x retry + backoff) because `GeminiClient` threw a bare `GeminiApiException("Gemini API returned 400.")` with no distinction and Google's message hidden in `ResponseBody`, invisible in logs. Fixes: (1) `BatchMaxTexts` 250→100 everywhere (config, `GeminiOptions` default, job doc comments) — the token ceiling only binds for unusually long chunks now; (2) `GeminiClient` exposes `MaxTextsPerBatchRequest = 100` as an internal const and `BatchEmbedContentAsync` throws `ArgumentOutOfRangeException` LOCALLY for >100 texts instead of sending a doomed request; (3) `EmbeddingBatchJob` clamps `Embedding:BatchMaxTexts` to the API cap at startup (misconfig warning, not per-batch failure); (4) NEW catch clause: permanent 4xx (400/404/422, not 408/429) drops the batch IMMEDIATELY — no retry, no backoff, no re-enqueue, worker keeps draining (a retry can never fix the request body); (5) 4xx exception messages now embed Google's `error.message` (via `TruncateForMessage`, ValueKind-guarded) so the actual cause is visible in log output instead of just "400". Verified: live API repro confirmed the cap, build clean, 179/179 unit tests pass incl. new regressions (>100-texts throws without HTTP call; permanent-400 drops batch in ONE attempt under 10s with zero retries). The 233 dropped rows are VectorId=NULL and will be picked up on next startup with the fixed cap.~~

- [x] ~~**[FIX-SQL-3]** Dedupe Scan SQL Timeout Killed Job Fatally (single 60s timeout → "Fatal error", 0 rows processed) — *Shads* `[Fixed: 2026-09-03] [Blocked by: FIX-EMB-4]`~~ — symptom: `GetUnembeddedChunksAfterAsync` (first page of the global dedupe scan) timed out at 60s, and since `BuildGlobalDedupeAsync` ran OUTSIDE any retry handling, one timeout aborted the entire job ("Fatal error in background loop", EmbeddingProgressState = error). Diagnosis: the query itself is fast — verified live via sqlcmd + STATISTICS TIME: 94ms warm against 42,805 rows / 28,763 unembedded, using the filtered index `IX_ACT_SECTION_CHUNK_VectorId_Null`; the 60s timeout was the dev box's chronic RAM pressure (1.0GB free of 7.8GB, Chrome ~1.2GB, SQL Express capped at 1024MB per FIX-SQL-2) causing a cold-buffer stall. The code bugs: (a) Phase-1 scan and the Phase-2 count had ZERO retry handling — a transient infra blip was instantly fatal; (b) Phase-3 follower stamping likewise unprotected, despite happening AFTER all API quota was spent. Fixes: (1) dedupe page reads wrapped in `ReadDedupePageWithRetryAsync` — bounded retry (5 attempts, 5s→120s exponential backoff) per page, safe because nothing is written at scan time; (2) `GetUnembeddedCountAsync` wrapped in `ReadWithRetryAsync` with a graceful FALLBACK (leaders+followers estimate) since it only feeds the ETA — an ETA blip must never kill a run; (3) follower stamping wrapped in `WriteWithRetryAsync` (bounded retry, then clean fatal so restart re-stamps safely); (4) all retry statuses surfaced bilingually via EmbeddingProgressState. Not changed: EF `CommandTimeout(60)` and the 1024MB SQL cap stay — the query is fast when buffers are warm; retries absorb the cold stalls. Verified: build clean, 179/179 unit tests pass. Operational note: the job is now timeout-resilient, but the dev box remains at ~1GB free RAM — closing Chrome tabs or pausing WSL2/Docker during a 28k-row embedding run is still the highest-leverage fix for wall-clock time.~~

- [x] ~~**[FIX-SQL-2]** SQL Server Resource Exhaustion (zombie process + RAM starvation) — *Shads* `[Fixed: 2026-09-03]`~~ — symptom: every query (even `SELECT TOP(1)` on USER PK) timing out at 60s, connection-pool pre-login handshake failures, app crash in SeedCategories; 29,869 unembedded chunks pending. Root cause (3-layer): (1) machine at 0.5GB free of 7.8GB RAM (WSL2/Docker ~3GB commit, 4 opencode instances, Chrome, Defender) — Windows paged sqlservr out of RAM (89MB working set, 2.5M page faults) so every data-page read was a hard disk fault; SQL Express max server memory was unlimited; (2) a zombie MuktoAin.Web process (started 4:05 AM — the crashed dotnet-run console left the child alive) kept re-hammering the dying server: EmbeddingBatchJob catch-all retried every 5s flat, each attempt holding pool connections through 60s command timeouts x EF EnableRetryOnFailure(5x); (3) exhausted pool slots starved new requests (UserRoleClaimsTransformation hits the DB per request) and the 29.8k unembedded backlog guaranteed heavy load on every restart. Fixes: (a) killed zombie PIDs 29924+11880 — SQL Server recovered instantly (sub-second queries); (b) capped max server memory at 1024MB via sp_configure; (c) EmbeddingBatchJob catch-all upgraded to exponential backoff (5s-10s-20s-40s-2min cap, reset on success); (d) added InternalsVisibleTo(MuktoAin.UnitTests) to Infrastructure.csproj; (e) fixed 3 real bugs surfaced by the half-compiled test files: ParseQuotaRetryInfo crashed on string-typed error bodies (now ValueKind-guarded), PackBatch gave duplicate chunks divergent Guid VectorIds while the Qdrant upsert wrote only one point (dangling vector ids — now one VectorId per content hash), ClearFourTwentyNineStreakIfClean was freeze-gated inconsistently vs its only call site (now unconditional per contract). Also repaired the half-finished uncommitted test files from a prior session (Moq expression-tree violation, callback-registered-after-invocation, invalid @type anonymous JSON payloads rebuilt via JsonObject, missing usings, stale exact-type assertion). Verified: solution builds clean, 173/173 unit tests pass, SQL Express responsive at 1024MB cap; prevention: free RAM before heavy runs, always check Get-Process MuktoAin.Web after a crashed dotnet-run console, backoff now prevents retry storms.~~

- [x] ~~**[FIX-SPEC-1]** Stale `text-embedding-004` References Purge (model switched to `gemini-embedding-001` mid-run, spec/docs never updated) — *Shads* `[Fixed: 2026-09-04]`~~ — the embedding model was changed in `appsettings.Development.json` to `gemini-embedding-001` but 16 references to the old `text-embedding-004` remained across code defaults, tests, spec, docs, and plan files, drifting documentation from reality. Verification performed first: live Qdrant Cloud queried — canonical collection `act_section_chunks` holds 21,701 points at **3072 dims** (fingerprint of gemini-embedding-001; text-embedding-004 is fixed 768-dim and cannot produce 3072-dim vectors), confirming every embedded vector is gemini-embedding-001 with no mixed-model vector space; legacy `act_section_chunks_dev` (265 points, 768-dim) identified as the text-embedding-004-era per-developer scratch collection, superseded and safe to delete. Fixes: replaced all 16 references with `gemini-embedding-001` — code default (`GeminiOptions.cs`), unit-test fixture (`GeminiClientTests.cs`), interface comment (`IEmbeddingService.cs`), `QdrantOptions.cs` dimension comment (dropped stale 768 note), `AGENTS.md` stack table (incl. 3072-dim MRL note), `.agent/spec/design.md` (architecture diagram + retrieval flow, 768→3072-dim), `README.md` stack table, `docs/deployment-guide.md` env-var default table, `Views/Admin/Analytics.cshtml` model status table, `plans/_Initial_setup_plan.md` (config sample + EnsureCollectionAsync note), `plans/Shads_plan.md` (3 config/code sketches), `plans/Tultul_plan.md` (VectorSize sketch 768→3072). Verified: `grep text-embedding-004` = 0 matches repo-wide; Domain + Infrastructure compile clean (full-solution build blocked only by DLL file locks from the actively running embedding job, not code errors); 179/179 unit tests pass. Resolved: stale Qdrant collection `act_section_chunks_dev` (265 pts, 768-dim — Tultul's T-1.11 era test vectors) KEPT per user decision 2026-09-04 — it is fully inert (no active config references it; 768-dim vectors are unusable by the 3072-dim app; no SQL linkage); canonical `act_section_chunks` (3072-dim) is production.~~

- [x] ~~**[FIX-EMB-5]** Embedding 429 Per-Minute Throttle Storms (quota unit = per-TEXT, not per-HTTP-call) — *Shads* `[Fixed: 2026-09-04] [Blocked by: FIX-EMB-4]`~~ — symptom: persistent "Throttled: per-minute quota hit" 429 sawtooth during background ingestion. Root cause (verified via Gemini API docs + Google's own 400-error wording "at most 100 requests can be in one batch"): the free-tier quota metric `embed_content_free_tier_requests` (100/min per project) counts EACH TEXT inside a `batchEmbedContents` call as one request — a 100-text batch spends 100 quota units in one HTTP call. `EmbeddingQuotaState` counted HTTP calls, so its gate (700/min) could never engage at real throughput (~2 workers × 100-text batches ≈ up to 12,000 texts/min): the job free-ran until every pool 429'd, AIMD-halved from 700 (~7 rounds before biting), backed off 30-60s, re-blasted, repeat. Compounding bugs: (a) `RecordQuotaExceeded` cleared the window queues but never reset the running token counter (permanent upward drift — would have zeroed the token budget the moment it was wired in); (b) `GetRemainingTokenBudget()` had ZERO production callers (the token gate was dead config); (c) dead catch clause for "bare 429 GeminiApiException" (unreachable — `SendAsync` converts every 429 into `GeminiQuotaExhaustedException` after parking the key). Fixes: (1) `EmbeddingQuotaState` window is now SUB-REQUEST WEIGHTED — `RecordSuccess(itemCount, tokenCost)` and `WaitAsync(itemCount)` track/gate on per-text quota units; deadlock guard admits a batch larger than a halved AIMD budget (refusing it would hang the job); `RecordQuotaExceeded` resets item+token counters with the queues; (2) `EmbeddingBatchJob` passes `TextsToEmbed.Count` through both calls and wires `_quota.GetRemainingTokenBudget()` into `PackBatchFromQueue`'s token ceiling (floor 1 — shrinks the batch rather than stalling); (3) dead 429 catch deleted; (4) `BatchMaxTexts` 100→25 (config + `GeminiOptions` default): one batch = 25% of a single project's 100 texts/min quota, keeping headroom for the ungated query-path embeds from the web app — verified by live probe (10×25-text batches, zero 429s); (5) budgets re-based to the real unit: `MaxRequestsPerMinute` 700→1200 items/min and `MaxTokensPerMinute` 700000→600000 (28 independent pools × 100 texts/min = 2,800 theoretical; 1200 conservative), config comments updated to document the per-text quota semantics. Deliberately NOT changed: ring-pointer rewind race in `AdvancePast` (benign at 28 pools, marked `ponytail:` note; generation-counter fix = over-engineering) and 250-500ms inter-key jitter on 429 (rejected — contradicts FIX-EMB-3's park-and-skip design; the next healthy pool shouldn't be punished for the throttled one). Verified: full solution builds clean; 183/183 unit tests pass incl. 5 new/updated regressions (budget-filling batch gates the next batch; batch>halved-budget still admitted; `RecordQuotaExceeded` resets token+item counters; token-window binding blocks; window tracking).~~

- [x] ~~**[FIX-EMB-6]** Concurrent Worker DbContext Thread-Safety Violation ("A second operation was started on this context instance") — *Shads* `[Fixed: 2026-09-05] [Blocked by: FIX-EMB-2]`~~ — symptom: EmbeddingBatchJob logged "Retrying after error (1x): A second operation was started on this context instance before a previous operation completed" during Qdrant indexing, burning Gemini quota on re-embeds via the transient-retry path. Root cause: `ExecuteAsync` created ONE DI scope and passed the same scoped `IActSectionChunkRepository` (wrapping one `AppDbContext`) to all `MaxConcurrentBatches` workers (default 2) running on concurrent `Task.Run` threads — DbContext is not thread-safe, and the shared `EmbeddingQuotaState.WaitAsync` gate *synchronized* the workers (they wait together, release together, land in the SQL-stamp phase together), so the collision fired reliably, not rarely. Fixes: (1) `WorkerLoopAsync` no longer takes the shared repo — each worker creates its own DI scope and resolves its own `IActSectionChunkRepository`/`AppDbContext` (mirrors the already-correct `BuildGlobalDedupeAsync` pattern; per-worker connection = 2 pool connections, trivial); (2) main `ExecuteAsync` scope retained only for the sequential pre-worker count read and Phase-3 follower stamping (single-threaded — safe); (3) unused `dedupePlan` worker param dropped. Regression test: `StartAsync_ConcurrentWorkers_ResolveDistinctScopedRepositoryPerWorker` runs with `MaxConcurrentBatches=2` and asserts exactly 4 scopes created (1 dedupe + 1 ExecuteAsync + 1 per worker), all resolving distinct repos, with all leader rows still stamped — fails if the fix regresses to a shared repo. Verified: Infrastructure build clean, 184/184 unit tests pass.

---

## 🚀 Checkpoint 2: Core Citizen Flow, RAG & Document Generation (45%)

### 1. Retrieval & RAG Context Assembly
- [x] ~~**[T-2.1]** `SimilaritySearchService.cs` (Qdrant Vector Retrieval) — *Tultul* `[Blocked by: S-1.4, S-1.8, T-1.11] [Unblocks: T-2.3]`~~ — implemented in `Infrastructure/VectorStore/SimilaritySearchService.cs`: embeds the query via `IEmbeddingService`, searches Qdrant via `IVectorStore`, re-hydrates full `ActSection` rows via `IActSectionRepository.GetBySectionIdsAsync`; dedupes multiple chunk hits from the same section down to the highest-scoring one; exposed via new `IVectorSectionSearch` Domain interface (mirrors T-2.2's `IKeywordSectionSearch`, the seam T-2.3's `RagContextBuilder` will use as its primary path); registered in `Program.cs`; extracted shared `SectionNumberResolver` (used by both this and `KeywordSearchService`) to `Infrastructure/Common/`; verified by 8 new Moq unit tests — full suite 80/80 passing
- [x] ~~**[T-2.2]** `KeywordSearchService.cs` (SQL FTS Fallback) — *Tultul* `[Blocked by: T-1.10] [Unblocks: T-2.3]` — implemented in `Infrastructure/Search/KeywordSearchService.cs` against `IActSectionRepository.FullTextSearchAsync`, exposed via new `IKeywordSectionSearch` Domain interface (Domain seam for T-2.3's `RagContextBuilder` fallback), registered in `Program.cs`; verified by 6 new Moq unit tests (mapping, quote-escaping, null SectionNumber, blank-query short-circuit) — full suite 38/38 passing~~
- [x] ~~**[T-2.3]** `RagContextBuilder.cs` (Vector-Primary with FTS Fallback) — *Tultul* `[Blocked by: T-2.1, T-2.2] [Unblocks: S-2.1]`~~ — implemented in `Application/Services/RagContextBuilder.cs` against the `IVectorSectionSearch`/`IKeywordSectionSearch` Domain seams: tries vector search first, falls back to keyword search (topK-capped) when the vector path throws or returns empty; blank query short-circuits without calling either; registered in `Program.cs` as `IRagContextBuilder`; verified by 7 new Moq unit tests (vector hit, empty fallback, throw fallback, topK propagation, blank query) — full suite 87/87 passing
- [x] ~~**[T-2.4]** `SearchService.cs` (Standalone Keyword Search for FR-7) — *Tultul* `[Blocked by: T-2.2] [Wires to E-2.4]` — implemented in `Application/Services/SearchService.cs` wrapping `IKeywordSectionSearch` (Domain abstraction, not the Infrastructure concrete) with pagination and optional Act filtering (resolved via `IActRepository.GetWithSectionsAsync` since `RetrievedSection` carries `ActTitle`, not `ActId`); registered in `Program.cs`; verified by 5 new Moq unit tests — full suite 46/46 passing~~
- [x] ~~**[T-2.5]** `CategoryService.cs` (Category Hierarchy for FR-6) — *Tultul* `[Blocked by: T-1.12] [Wires to E-2.5]` — implemented as thin CRUD passthrough over `IRepository<CaseCategory>` in `Application/Services/CategoryService.cs`; note: `CaseCategory` is a flat lookup table (no `ParentCategoryId`), so there's no actual tree despite the task label — registered in `Program.cs`; verified by 3 new Moq unit tests — full suite 41/41 passing~~
- [ ] **[T-2.6]** Checkpoint 2 Search Infrastructure Exit Gate — *Tultul* `[Blocked by: T-2.1 to T-2.5]`

### 2. AI Orchestration, Prompt Assembly & Logging
- [x] ~~**[S-2.1]** `PromptAssembler.cs` (Context Assembly & Grounding) — *Shads* `[Blocked by: T-2.3, S-1.5] [Unblocks: S-2.2]`~~ — implemented `IPromptAssembler` and `PromptAssembler` in Application layer; builds grounded prompts from retrieved sections, scenario mappings, and language disclaimers; verified with unit tests
- [x] ~~**[S-2.2]** `AiOrchestrationService.cs` (Gemini Flash Pipeline) — *Shads* `[Blocked by: S-2.1, S-1.3, S-1.6, S-2.6] [Unblocks: S-2.3, S-2.4, A-2.2]`~~ — implemented `IAiOrchestrationService` and `AiOrchestrationService`; orchestrates cache checks, RAG context retrieval, prompt assembly, Gemini generation, disclaimer injection, token estimation, AI audit logging, and citation persistence in `CASE_ACT_REFERENCE`; verified with unit tests
- [x] ~~**[S-2.3]** `RightsExplanationService.cs` (Explain My Rights) — *Shads* `[Blocked by: S-2.2] [Unblocks: A-2.2, Wires to E-2.2]`~~ — implemented `IRightsExplanationService` and `RightsExplanationService` facade returning `RightsExplanationDto`; verified with unit tests
- [x] ~~**[S-2.4]** `AiLogService.cs` (Audit Logging & Token Tracking) — *Shads* `[Blocked by: T-1.4, S-2.2] [Unblocks: S-2.7]`~~ — implemented `IAiLogService` and `AiLogService` persisting latency, model, tokens, and response to `AI_LOG` table; verified with unit tests
- [x] ~~**[S-2.6]** Polly Resilience Policies (Retry, Key Rotation & Fallback) — *Shads* `[Blocked by: S-1.3] [Unblocks: S-2.2]`~~ — implemented `GeminiResiliencePolicies` (timeout -> circuit breaker -> exponential backoff retry) and registered in DI; verified with unit tests
- [x] ~~**[S-2.7]** AI Logging PII Redaction & Audit Safety — *Shads* `[Blocked by: S-2.4]`~~ — integrated regex-based citizen problem description redaction in `AiLogService` before saving to `AI_LOG`; verified with unit tests

### 3. Case Lifecycle, Document Generation & Lawyer Review Gate
- [x] ~~**[A-2.1]** `CaseService.cs` (Intake, State Machine, Tracking Code) — *Arpita* `[Blocked by: T-1.4, T-1.12, A-1.1] [Unblocks: S-2.5, A-2.4]` — implemented with `SubmitCaseAsync` returning `CaseSubmissionResultDto(CaseId, AnonymousTrackingCode)` (GUID shown once for FR-8), guest/citizen/lawyer/admin access rules in `GetCaseDetailAsync` (explicit null-compare guard), tuple-switch state machine incl. `Finalized→Submitted` re-open row, name lookups via injected `IRepository<CaseCategory>`/`IRepository<District>`; verified by 10 new Moq unit tests~~
- [x] ~~**[S-2.5]** Wire `EncryptionService` into `CaseService` for PII — *Shads* `[Blocked by: S-1.7, A-2.1]`~~ — wired `IEncryptionService` into `CaseService` for encrypt-on-write and decrypt-on-read of `Title` and `Description` with graceful plaintext fallback; verified with unit tests
- [x] ~~**[A-2.2]** `DocumentGenerator.cs` (Core Document Generation Engine) — *Arpita* `[Blocked by: S-2.2, S-2.3] [Unblocks: A-2.3]`~~ — implemented `IDocumentTemplate` and `DocumentGenerator` in `MuktoAin.Application.Documents`; delegates to matching template via DI auto-discovery dictionary; category-to-document-type mapping verified against seed categories; verified with unit tests
- [x] ~~**[A-2.3]** `LabourComplaintTemplate.cs` (First Structured Template) — *Arpita* `[Blocked by: A-2.2] [Unblocks: A-2.4]`~~ — implemented `LabourComplaintTemplate` in `MuktoAin.Application.Documents.Templates` following Bangladesh District Labour Court complaint format with cited statutory sections and bilingual disclaimer stamp (Surface 3 of 3); verified with unit tests
- [x] ~~**[A-2.4]** `DocumentService.cs` (Document CRUD, Lifecycle & Lockout) — *Arpita* `[Blocked by: A-2.3, A-2.1] [Unblocks: A-2.5, A-2.7]`~~ — implemented `DocumentService` in `MuktoAin.Application.Services` with `GenerateDocumentAsync` creating `Draft` status docs with immutable `ContentDraft`, explicit District/Category navigation resolution, preview retrieval, and status updates (`Approved`, `EditedApproved`, `Rejected`); registered in DI; verified with unit tests
- [ ] **[A-2.5]** `PdfExportService.cs` with QuestPDF (Bengali Font) — *Arpita* `[Blocked by: E-1.3, A-2.4] [Wires to E-2.6]`
- [x] ~~**[A-2.6]** `LawyerVerificationService.cs` (Bar Council Verification) — *Arpita* `[Blocked by: T-1.4, T-1.12] [Unblocks: A-2.7]` — implemented Apply/Verify/GetPendingApplications with duplicate-application guard, VerifiedByAdminId + VerifiedAt stamping on decision; verified by 6 new Moq unit tests~~
- [ ] **[A-2.7]** `LawyerReviewService.cs` (Queue, Claim Race Guard, Decisions) — *Arpita* `[Blocked by: A-2.6, A-2.4] [Wires to E-2.7]`
- [ ] **[A-2.8]** Checkpoint 2 Document & Review Exit Gate — *Arpita* `[Blocked by: A-2.1 to A-2.7]`

### 4. Frontend Citizen Views & Contract Handoff
- [x] ~~**[E-2.1]** Citizen Case Intake View (`/Case/Submit`) — *Erin* `[Mock Ready -> Wires to A-2.1]`~~
- [x] ~~**[E-2.2]** Legal Analysis & Rights Explanation View (`/Case/Result`) — *Erin* `[Mock Ready -> Wires to S-2.3]`~~
- [x] ~~**[E-2.3]** Anonymous Case Tracking View (`/Case/Track`) — *Erin* `[Mock Ready -> Wires to A-2.1]`~~
- [x] ~~**[E-2.4]** Acts Keyword Search View (`/Search`) — *Erin* `[Mock Ready -> Wires to T-2.4]`~~
- [x] ~~**[E-2.5]** Act Category Browsing & Detail Views (`/Category`) — *Erin* `[Mock Ready -> Wires to T-2.5]`~~
- [x] ~~**[E-2.6]** Generated Document Preview View (`/Document/Preview`) — *Erin* `[Mock Ready -> Wires to A-2.4]`~~ — implemented `DocumentController.cs` with `Preview` and `Download` actions, `DocumentPreviewViewModel`, and `Views/Document/Preview.cshtml` with parchment styling, Surface 3 disclaimer stamp, and lawyer approval PDF gate
- [x] ~~**[E-2.7]** Lawyer Verification & Review Views (`/Lawyer/Queue`, `/Lawyer/Review`) — *Erin* `[Mock Ready -> Wires to A-2.7]`~~
- [x] ~~**[E-2.8]** `api-contracts.md` Specification & `.resx` Resource Files — *Erin* `[Unblocks: S-3.5]`~~ — created `docs/api-contracts.md` specifying all controller-to-service contracts, and populated `SharedResource.bn.resx` / `SharedResource.en.resx` with UI strings
- [x] ~~**[E-2.9]** Checkpoint 2 Frontend Views Exit Gate — *Erin* `[Blocked by: E-2.1 to E-2.8]`~~ — all 8 Checkpoint 2 citizen, search, category, document, and lawyer review views implemented and verified; live HTTP audit passing across all routes
- [ ] **[S-2.8]** Checkpoint 2 Overall Citizen Flow & RAG Integration Exit Gate — *Shads* `[Blocked by: S-2.1 to S-2.7, T-2.6, A-2.8, E-2.9]`

---

## 🏛️ Checkpoint 3: Review Gate, Admin, Evaluation & Delivery (25%)

### 1. Document Templates, Admin Services & Schema Enhancements
- [ ] **[A-3.1]** Complete All 4 Document Templates (GD, RTI, Consumer) — *Arpita* `[Blocked by: A-2.2]`
- [ ] **[A-3.2]** `AdminAnalyticsService.cs` (KPIs, Funnels, Workloads) — *Arpita* `[Blocked by: T-1.12] [Wires to E-3.1]`
- [ ] **[T-3.1]** `ActsManagementService.cs` (Admin CRUD & SHA256 Re-indexing) — *Tultul* `[Blocked by: T-1.8, S-1.8] [Wires to E-3.2]`
- [ ] **[T-3.2]** `ScenarioMappingService.cs` (Admin Keyword Boosts for FR-18) — *Tultul* `[Blocked by: T-1.12] [Wires to E-3.2]`
- [x] ~~**[S-3.6]** `UserManagementService.cs` (Admin Role Management) — *Shads* `[Blocked by: S-1.1] [Wires to E-3.3]`~~ — implemented `IUserManagementService` and `UserManagementService` wrapping `UserManager<User>` with suspension and admin protection guardrails; registered in DI; verified with unit tests

### 2. Admin Frontend Views & Integration Wiring
- [x] ~~**[E-3.1]** Admin Dashboard & Analytics Views (`/Admin/Analytics`) — *Erin* `[Blocked by: E-1.1] [Wires to A-3.2]`~~
- [ ] **[E-3.2]** Admin Acts Management & Scenario Views (`/Admin/Acts`) — *Erin* `[Blocked by: E-1.1] [Wires to T-3.1, T-3.2]`
- [ ] **[E-3.3]** Admin User Management Views (`/Admin/Users`) — *Erin* `[Blocked by: E-1.1] [Wires to S-3.6]`
- [ ] **[E-3.4]** Final Integration: Wiring Controllers to Real Services — *Erin* `[Blocked by: All Backend Services]`
- [ ] **[E-3.5]** Checkpoint 3 Frontend Integration Exit Gate — *Erin* `[Blocked by: E-3.1 to E-3.4]`

### 3. QA Benchmark, Testing & Hardening
- [ ] **[S-3.1]** QA Benchmark Dataset Loader (2,165 Questions) — *Shads* `[Blocked by: T-2.3, A-2.2] [Unblocks: S-3.2]`
- [ ] **[S-3.2]** Benchmark Runner (Zero-Shot Baseline Evaluation) — *Shads* `[Blocked by: S-3.1] [Unblocks: S-3.3]`
- [ ] **[S-3.3]** Few-Shot IRAC Prompt Assembly & Re-Evaluation — *Shads* `[Blocked by: S-3.2]`
- [ ] **[A-3.3]** Business Logic Unit Tests & Security Edge-Case Tests — *Arpita* `[Blocked by: A-2.1 to A-2.7]`
- [ ] **[A-3.5]** `ModerationService.cs` (Submission Blocklist Filter) — *Arpita* `[Blocked by: T-1.12]`
- [ ] **[A-3.6]** `docs/attribution-CC-BY-SA-4.0.md` (Acts + QA Dataset Licenses) — *Arpita* — *(no blockers)*
- [ ] **[T-3.3]** Repository & DB Integration Tests — *Tultul* `[Blocked by: T-1.12]`
- [ ] **[T-3.5]** `docs/architecture.md` (Clean Arch, ERD, Retrieval Pipeline) — *Tultul* — *(no blockers, update as architecture evolves)*
- [ ] **[A-3.4]** Checkpoint 3 Arpita Exit Gate — *Arpita* `[Blocked by: A-3.1 to A-3.3, A-3.5, A-3.6]`
- [ ] **[T-3.4]** Checkpoint 3 Tultul Exit Gate — *Tultul* `[Blocked by: T-3.1 to T-3.3, T-3.5]`

### 4. Packaging, Localization & Final Release
- [ ] **[S-3.4]** Multi-Stage Dockerfile & GitHub Actions CI/CD Pipeline — *Shads*
- [ ] **[S-3.5]** `RequestLocalizationMiddleware.cs` & Resource Files Wiring — *Shads* `[Blocked by: E-2.8]`
- [ ] **[S-3.8]** `docs/deployment-guide.md` (Prereqs, Local Setup, Docker, Azure, Secrets) — *Shads* `[Blocked by: S-3.4]`
- [ ] **[S-3.9]** Root `README.md` (Mission, Stack, Quick Start, Attribution, Disclaimer) — *Shads* `[Blocked by: S-3.4, S-3.8]`
- [ ] **[S-3.7]** Checkpoint 3 Final Release & Academic Delivery Gate — *Shads* `[Blocked by: All Checkpoint 3 Tasks]`

---

## ⚡ Direct Handoff Summary Table

| Handoff # | Blocked Task | Assigned | Blocked By / Waiting On | Delivered By |
|---|---|---|---|---|
| **H-1** | Solution Scaffold `[T-1.1]` | **Tultul** | Initial repo & configs `[S-Setup.1]` | **Shads** |
| **H-2** | DTOs `[A-1.1]` | **Arpita** | Enums & Entities `[T-1.2, T-1.3]` | **Tultul** |
| **H-3** | Identity `[S-1.1]` | **Shads** | `User.cs`, `AppDbContext.cs`, SSMS Schema `[T-1.3, T-1.5, T-1.6]` | **Tultul** |
| **H-4** | Embedding Batch `[S-1.8]` | **Shads** | Chunk pipeline & `QdrantVectorStore` `[T-1.9, T-1.11]` | **Tultul** |
| **H-5** | `SimilaritySearchService` `[T-2.1]` | **Tultul** | `GeminiEmbeddingService` & Qdrant chunks `[S-1.4, S-1.8]` | **Shads** |
| **H-6** | `PromptAssembler` / AI `[S-2.1, S-2.2]` | **Shads** | `RagContextBuilder` `[T-2.3]` | **Tultul** |
| **H-7** | `DocumentGenerator` `[A-2.2]` | **Arpita** | `AiOrchestrationService` & Rights DTO `[S-2.2, S-2.3]` | **Shads** |
| **H-8** | `PdfExportService` `[A-2.5]` | **Arpita** | Noto Sans Bengali font `[E-1.3]` | **Erin** |
| **H-9** | Localization Middleware `[S-3.5]` | **Shads** | `.resx` resource files `[E-2.8]` | **Erin** |
| **H-10** | QA Benchmark Runner `[S-3.1]` | **Shads** | Full RAG pipeline + Document Generator `[T-2.3, A-2.2]` | **Tultul & Arpita** |
| **H-11** | Full View Integration `[E-3.4]` | **Erin** | Finished backend service implementations | **All Teammates** |
